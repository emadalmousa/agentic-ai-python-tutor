# OpenAI (optional) — wenn leer oder keine Credits → automatisch Ollama
OPENAI_API_KEY=sk-...
LLM_MODEL=gpt-4o

# Ollama (Fallback, läuft lokal)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2
